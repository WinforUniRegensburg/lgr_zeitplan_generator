# Clone the repository
$ git clone https://github.com/WinforUniRegensburg/lgr_zeitplan_generator

# Go into the repository
$ cd lgr_zeitplan_generator

# Install the dependencies and run
$ npm install && npm start

# Run in Mac OS
$ npm run package-mac

# Run in Windows
$ npm run package-win 

# Run in Linux
$ npm run package-linux